[package]
edition = "2021"
name    = "api-lib"
version = "0.1.0"

[dependencies]
axum = { version = "0.6", features = ["tracing"] }

# tracing
tracing = "0.1"

[dev-dependencies]
tokio = { version = "1.0", features = ["full"] }
