[package]
edition = "2021"
name    = "api-lib"
version = "0.1.0"

[dependencies]
axum = { workspace = true }

# tracing
tracing = { workspace = true }

[dev-dependencies]
tokio = { version = "1.0", features = ["full"] }
